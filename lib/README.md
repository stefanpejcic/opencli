Shared helper scripts, sourced by other opencli scripts (`. /usr/local/opencli/lib/<file>.sh`) or, in the case of `completion.bash`, symlinked into `/etc/bash_completion.d/`.

None of these are meant to be run directly.
